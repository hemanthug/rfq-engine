[project]
name = "rfq-engine-backend"
version = "0.1.0"
description = "Upload-to-quote backend with CAD parsing, preview mesh generation, feature extraction, and CNC budgetary pricing"
requires-python = "==3.10.*"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
